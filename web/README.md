# Frontend Dashboard

Static frontend for visualizing inference results from the backend API.

## Configure the API base URL

By default, the frontend uses `http://localhost:5000`. You can override this in
`web/config.js`.

## Serve locally

```bash
cd web
python3 -m http.server 8000
```

Open `http://localhost:8000` in your browser.
